**日期**：2026-09-26（Asia/Shanghai）

**范围**：Cloudex 当日用户报告的问题、直接相关的移动端能力缺口和性能问题。记录对应 `fix/codex-session-history` 分支的修复；不是上游 `2O48/Cloudex` 已合并或所有设备均已验收的声明。

**Codex 基线**：研究初期本机 CLI 为 `codex-cli 0.157.0`；本轮实际运行的受管 daemon 为 `0.157.1`，Desktop 内置二进制为 `0.155.0-alpha.16.4`。以下“Codex 机制”是这些版本在本机文件及 Cloudex 交互中的观察结果，不应当作未来版本的稳定协议。

**升级索引**：升级 Codex 后先核对三件事，再验证手机端：

1. `~/.codex/sessions` 是否仍使用 `rollout-...-<thread-id>.jsonl`，续写是否仍使用 `rollout-...-<thread-id>_<continuation-id>.jsonl`；检查 `session_meta.payload.id` 与文件名中的原任务 ID 是否一致。不要用文件名末尾的 UUID 当作任务 ID。
2. `app-server` 的 `thread/resume`、`thread/unsubscribe`、`thread/started` 与 `turn/start` 是否仍有相同的 writer/订阅语义。尤其不能从收到 `thread/started` 推断 Cloudex 拥有 writer，也不能为了只读浏览主动 resume。
3. 运行 `npm test`，再分别通过本机与手机实际使用的转发地址检查 `/api/projects`、`/api/threads/:id?limit=12`、`before` 分页和 `/message-index`；用一条桌面续写的任务确认手机能看到用户消息与最终回复。SSE 关闭/租约还需用真机和代理链路检查。

# 1. 多台主机缺少统一任务概览

- **出现情况 / Case**：保存了多台 PC 或服务器后，手机要逐台切换才能知道哪台在线、有哪些运行任务或待审批请求；同名项目也难以分清属于哪台主机。这是当天提出的移动端能力缺口，不是 Codex CLI 回归。
- **修复**：增加保存的主机 profile、主机概览与切换入口。`apps/ios-native/CloudexNative/AppViewModel.swift:1213` 管理 profile，`:1490` 拉取概览；`apps/ios-native/CloudexNative/SidebarView.swift:370` 展示各主机状态和项目。
- **验证 / 提交**：随 `6228d85` 实现并完成 iOS 构建/安装；之后的并发加速见第 8 项。此能力的最终真机视觉验收不在服务端单测范围内。

# 2. 手机内容进入任务的路径不完整

- **出现情况 / Case**：手机里的照片、截图、网页或文字不能方便地从系统分享菜单选主机和会话后作为可编辑草稿使用；语音输入和快速查看最近任务也缺入口。这同样是产品能力缺口，不属于 Codex 文件格式问题。
- **修复**：分享扩展写入 App Group 收件箱，由 `apps/ios-native/CloudexShare/ShareViewController.swift:4` 接收，`apps/ios-native/CloudexNative/SidebarView.swift:1092` 选目标；`apps/ios-native/CloudexNative/AppViewModel.swift:1287` 处理分享项，`:2149` 上传手机图片；`apps/server/src/server.js:1311` 校验并保存图片。`apps/ios-native/CloudexNative/SpeechInputController.swift:5` 负责语音转写，`apps/ios-native/CloudexWidget/CloudexCurrentTaskWidget.swift:56` 展示最近一次同步的任务快照。
- **验证 / 提交**：`6228d85`；服务端图片格式/大小边界由 `apps/server/test/upload-image.test.js` 覆盖。小组件是最近一次同步快照，不承诺后台实时状态。

# 3. 手机关闭后，电脑会话仍被 Cloudex 占用

- **出现情况 / Case**：手机 App 已关闭，Codex Desktop 的同一任务仍暂时无法继续对话；点“重试”在占用释放前无效。手机连接结束不等于 Cloudex 对 Codex 的订阅已释放。
- **原因与 Codex 机制**：在本机 `0.157.0` 的控制链路里，Cloudex 对任务执行 `thread/resume` 后需要显式 `thread/unsubscribe`。仅断开手机 SSE 而保留 app-server 订阅，会让 Desktop 继续遇到 writer 占用。
- **修复**：iOS 进入后台时停止全局和任务 SSE，见 `apps/ios-native/CloudexNative/AppViewModel.swift:1174`；服务端最后一个任务 SSE 关闭后延迟 250 ms 释放订阅，见 `apps/server/src/server.js:558`；`apps/server/src/codex-client.js:343`、`:358` 串行处理 resume/unsubscribe，避免刚重连就误释放；发消息、新建或 fork 后也在 `finally` 安排释放。
- **验证 / 提交**：`6228d85`；`apps/server/test/turn-lifecycle.test.js:91`、`:104`、`:120` 覆盖释放、快速重连与竞态。用户随后确认 Desktop 能再次输入。

# 4. 强退或代理未及时关闭 SSE，释放仍可能拖很久

- **出现情况 / Case**：手机实际已离开任务，但通过 VPS/反向代理的长连接未及时触发服务端 `close`，第 3 项的“最后一个 SSE 关闭”条件迟迟不成立。
- **修复**：新版 iOS 在前台选中任务时约每 5 秒调用 `/api/threads/:id/lease`，见 `apps/ios-native/CloudexNative/AppViewModel.swift:1470`；服务端在约 20 秒无续租后销毁静默 SSE 并安排 unsubscribe，见 `apps/server/src/server.js:571`、`:1426`。这给异常断线增加了有界的回收路径，不依赖 TCP 立即通知。
- **验证 / 提交**：`ee246ea`；`apps/server/test/stream-lease.test.js:6` 验证续租可延长连接、停止续租后连接会关闭。20 秒是服务端租约值，不是保证 Desktop 在所有网络条件下恰好 20 秒恢复。

# 5. 租约初版会误断开旧版 iOS 客户端

- **出现情况 / Case**：第 4 项的初版曾给所有任务 SSE 启用 20 秒超时；尚未升级、不会发送 lease 的旧版 App 会被周期性断开。
- **修复**：只有显式使用 `?lease=1` 打开的新客户端才启用租约计时；旧客户端保持原有 SSE 行为。入口在 `apps/ios-native/CloudexNative/AppViewModel.swift:2371`，服务端判断在 `apps/server/src/server.js:583`、`:1439`。
- **验证 / 提交**：`a93a7f5`；租约单测改为显式选择 leased 连接。旧版客户端未在真机上做单独端到端测试，因此“旧版完全兼容”仍以代码路径核对为依据。

# 6. 未在电脑打开的任务仍提示 active writer

- **出现情况 / Case**：手机发送消息时出现 `thread ... already has an active writer`，即使用户并未在 Codex Desktop 打开该对话。打开手机会话本身不应抢占 Codex writer。
- **原因与 Codex 机制**：先前 Cloudex 为只读 SSE 调用了 `thread/resume`，还把收到的 `thread/started` 通知误当成自己已订阅。通知描述任务事件，不证明 writer 所有权。
- **修复**：只读任务 SSE 只返回 `read-only`，不 resume；仅发送 turn 时 resume。删除 `thread/started` 对 `subscribedThreads` 的错误赋值；真正冲突转为明确的 HTTP 409，而不是原始 502。位置：`apps/server/src/server.js:1457`、`:1590`、`:328`，`apps/server/src/codex-client.js:304`。
- **验证 / 提交**：`769a0da`；`apps/server/test/passive-thread-stream.test.js:19`、`:35` 和 `apps/server/test/turn-lifecycle.test.js:85` 覆盖只读浏览、409 与错误所有权记录。

# 7. 打开已有会话时，本地缓存被模型请求挡住

- **出现情况 / Case**：手机已有会话缓存，但点开仍先显示“正在打开对话”，尤其经 VPS 时会等模型接口往返；即使缓存已到手，远端刷新也可能被已锁定的静态布局挡住。
- **修复**：模型列表只在需要时后台加载，不再每次强制请求；本地详情和消息索引加载后即可解除打开状态，网络详情随后覆盖缓存；详情变化时刷新已完成会话的内容快照。位置：`apps/ios-native/CloudexNative/AppViewModel.swift:1585`、`:1643`，`apps/ios-native/CloudexNative/ContentView.swift:310`。
- **验证 / 提交**：`3beb396`；iOS 模拟器及真机构建通过，新版已安装到 iPhone。没有自动化真机首屏计时，速度收益属于调用链改进，不宣称固定毫秒数。

# 8. 首页及多主机概览串行网络等待

- **出现情况 / Case**：`refresh()` 先请求 health，再依次请求项目和审批；多主机概览又逐台依次执行 health、项目、审批。VPS 每次往返会叠加为明显等待。
- **修复**：项目接口直接承担连通性判断，项目和审批并发；各主机概览使用 task group 并发拉取，单台主机仍保留局域网/Tailscale 候选地址回退。位置：`apps/ios-native/CloudexNative/AppViewModel.swift:1380`、`:1490`、`:1506`。
- **验证 / 提交**：`3beb396`；iOS 模拟器和真机构建通过。本机 API 与 VPS 往返曾分别测量；这里不把单次网络样本当成稳定性能承诺。

# 9. 超长 Codex 会话每次读取都重新解析 JSONL

- **出现情况 / Case**：服务端读取最新 12 个回合时，原先仍会读取并解析整份 JSONL；长任务频繁刷新会重复做同样工作。
- **修复**：`apps/server/src/cli-sessions.js:724` 串行化详情读取，`:732` 从上次完整行的字节偏移读取新增内容，`:773` 用文件身份、大小与修改时间决定能否复用解析状态。第 10 项后来把同一缓存扩展到一组续写文件。截断、同长度改写和文件替换会重新解析；同一 inode 上“改写成更长文件”仍按 Codex 追加写入处理，是当前明确边界。
- **验证 / 提交**：`3beb396`、`40e7ea4`；`apps/server/test/turn-lifecycle.test.js:9` 覆盖追加、半行和重写。一次 10 MB 本地样本首次解析约 53 ms，原文件未变化时二次读取低于 1 ms；首次读取仍需完整解析。

# 10. “阅读JH”续写回合在手机上消失

- **出现情况 / Case**：手机发送“今天是什么5层运”后收到完成通知，但会话页看不到提问或回复；Desktop 两条都可见。修复前 Cloudex 详情 API 只返回到较早的一条七月时间追问。
- **原因与 Codex 机制**：本机 `0.157.0` 把后续回合写进 `rollout-...-<thread-id>_<continuation-id>.jsonl`；`session_meta.payload.id` 仍是原任务 ID，续写文件只含后半段。旧正则只接受末尾直接为 `<thread-id>.jsonl` 的文件，导致整个续写文件被 `findSessionFiles()` 忽略。完成通知来自实时事件，而历史页走文件读取，所以两者可不一致。本机检查到 16 个这类文件，首条元数据中的 ID 均与原任务 ID 一致。
- **修复**：`apps/server/src/cli-sessions.js:8` 识别可选的续写 ID，`:700` 返回原任务 ID，`:884` 按原 ID 分组，`:760` 用同一解析状态依时间顺序合并所有片段，`:902` 读取整组详情，保留分页、归档、去重和追加缓存。`apps/server/src/windows-cli.js:328` 排除续写文件作为“新任务”，避免误认其他任务的续写。
- **验证 / 提交**：`40e7ea4`；`apps/server/test/cli-continuation.test.js:13` 覆盖跨文件合并、追加与归档；最终服务端 28 项测试通过。真实任务恢复为 14 个回合，项目列表仅一条；本机及 VPS 转发的详情 API 均返回最新提问和最终回复，`before` 分页无重叠，消息索引也包含两条。Mac 上服务已重启；该提交只改服务端，手机无需重装。最终手机屏幕没有自动截图验收，重新进入会话后应由 App 拉取新详情。

# 11. 手机点击发送后几秒才出现自己的消息，等待状态只有转圈

- **出现情况 / Case**：在已有会话或新建会话点击发送，右侧用户消息要等 HTTP 请求返回、会话详情再次加载后才出现；发送按钮和聊天底部都显示无说明的转圈，用户无法判断消息是否已提交或是否在等回复。
- **原因**：`submitPrompt()` 原先等服务端确认后才清空草稿，界面没有本地待发送消息；聊天底部的 `ProgressView` 没有区分发送与执行阶段。若服务端开始执行后历史快照尚未写入，刷新也会延后消息出现。
- **修复**：`apps/ios-native/CloudexNative/AppViewModel.swift` 的 `submitPrompt()` 在请求前插入右侧临时用户消息并清空草稿，服务端确认后标记“已发送”，失败时恢复草稿；`buildMessages()` 按服务端 turn ID 和真实用户项去重，避免快照出现时双份消息。`apps/ios-native/CloudexNative/Models.swift` 只解码发送所需的 turn ID。`apps/ios-native/CloudexNative/ContentView.swift` 显示“正在发送 / 等待回复”的文字状态并移除发送按钮、聊天底部的无说明转圈；临时项从快照消失时不再被滚动列表保留。
- **验证**：服务端 29 项测试通过，iOS 设备及 iPhone 18 Pro 模拟器 Debug 构建通过；模拟器成功安装并启动。模拟器没有配对服务器，因此未做端到端发送。iPhone 14 Pro 当前被 Xcode 标为 `unavailable`，尚未完成该版本的真机点按验收；即时回显及动画流畅度仍需连接真机复核。

# 12. 手机还开着会话时，Codex writer 可能长期占用；Desktop 只看会话也提示冲突

- **共享服务研究补充**：发现 Desktop 内置版本实际为 `0.155.0-alpha.16.4`，与 Cloudex daemon `0.157.0` 不同。两版分别通过“同一个 app-server、两个客户端、同一任务交替发送”的隔离测试；Desktop 安装包存在 WebSocket 连接覆盖入口，后台 owner 回收存在 3 小时阈值。详见 [共享 app-server 研究](2026-09-26-shared-app-server-research.md)。这修正了此前把不同 app-server 之间的 writer 冲突笼统说成“所有多客户端必须等上游支持”的结论；完整 Desktop 接入仍未验收。

- **出现情况 / Case**：手机发送提示“此会话正由其他客户端占用”；电脑端未发送消息，仅打开会话。另一方向上，手机保持 SSE 连接时，Cloudex 自己也可能在任务已结束后继续持有 writer，使电脑无法立刻接手。
- **原因与 Codex 机制**：在本机 `codex-cli 0.157.0` 的 `thread/resume` / `thread/unsubscribe` 排他写入机制下，只读 SSE 不需要 writer；但服务端原先用“是否仍有手机 SSE 订阅者”决定是否调用 `thread/unsubscribe`，所以 SSE 长连接会阻止释放。Codex Desktop 自己持有 writer 的场景仍受上游排他规则限制，Cloudex 不能安全地强抢或并发写入同一会话。
- **修复**：`apps/server/src/server.js` 将 Cloudex 发起的回合记录在 `ownedRunningThreads`，在终止事件后安排 `thread/unsubscribe`，不再让手机只读 SSE 阻止释放；发送失败及无任务的新会话仍安排释放。409 文案提示另一客户端占用，并说明仅切换电脑端对话不会释放。`apps/server/test/passive-thread-stream.test.js` 覆盖 SSE 仍打开时执行释放。
- **验证**：服务端 29 项测试通过，含新增长连接释放用例；Mac LaunchAgent 已重启，服务监听 `8890`，认证后的健康检查返回 `200`。没有向用户真实会话注入测试消息，也未证明 Desktop 自身的 writer 能在保持该会话打开的同时共享；要并发写入同一会话仍需上游支持。
- **后续核查**：用户在电脑切换会话后仍被占用。Desktop 日志显示离开任务时仅将 `thread_stream_view_activity_changed` 标为 `active=false`，旧任务的 `streamRole` 仍为 `owner`；在该次切换附近没有对应的 `thread/unsubscribe`。Codex CLI `0.157.0` 生成的 `ThreadResumeParams` 只有常规 resume 配置，没有强制接管 writer 的字段。因此此前“切换会话后重试”的建议不成立；需识别具体占用客户端并由其释放订阅，不能让 Cloudex 盲目强抢。

# 13. Desktop 长时间占用 writer，手机无法接手

- **出现情况 / Case**：电脑只打开过同一任务，即使切换走，手机仍报“此会话正由其他客户端占用”。此前 Cloudex 自己的订阅释放修复只能解决手机占用电脑，不能强制 Desktop 释放其独立 app-server 的 writer。
- **机制与版本**：Desktop 内置 `0.155.0-alpha.16.4`，Cloudex 受管 daemon 当前为 `0.157.1`；两个独立 app-server 共享历史文件但各自持有会话。Desktop 当前安装包读取 `CODEX_APP_SERVER_WS_URL`，可在**下次启动**改接 Cloudex 使用的 daemon。这个入口不是稳定公开 API，升级后须重查。详见 [研究与验证](2026-09-26-shared-app-server-research.md)。
- **修复位置**：`apps/server/bin/desktop-bridge.js` 提供只监听本机的令牌化 WebSocket→Unix socket 桥；本机用户级 LaunchAgent `~/Library/LaunchAgents/com.pans0020.cloudex-desktop-bridge.plist` 启动桥并设置 Desktop 下次启动的连接变量。不删锁、不修改 Codex 历史文件。
- **验证与边界**：`apps/server/test/desktop-bridge.test.js` 覆盖拒绝错误令牌及双向帧转发；真实 daemon 的只读 `initialize` 成功；服务端 30 项测试通过。当前 Desktop 仍是旧 stdio 连接，本次会话结束并完整重启 Desktop 后才可验收 GUI、审批、工具及真实任务的双向发送。在此之前不能宣称 writer 冲突已端到端解决。

# 14. 聊天消息缺少层次，附件和语音挤占输入行

- **出现情况 / Case**：手机聊天页中的用户与 Codex 消息区分不够明显，连续对话近似记事本；图片、语音与输入框、发送键同列，输入空间受挤压。
- **修复位置**：`apps/ios-native/CloudexNative/ContentView.swift` 增加消息间距，用浅青绿用户气泡与次级背景 Codex 气泡、细边框和角色色点建立区分；附件、语音、命令改为输入框上方一排，输入框与发送按钮独占下一排，调整滚动底部预留高度。
- **验证与边界**：iOS 模拟器和 iPhone 14 Pro 签名 Debug 构建通过，新版已安装到真机；模拟器这台 Xcode 未提供可打开的 Simulator.app，未完成聊天页截图核对。应在手机实际打开长对话，检查窄屏、键盘弹出、附件显示和长消息布局。

**前一阶段交付记录（第 15–19 项之前）**：此前提交 `6228d85`、`ee246ea`、`a93a7f5`、`769a0da`、`3beb396`、`40e7ea4` 已推送到个人 fork 的 `fix/codex-session-history`，当时的 iOS 版本已安装到 iPhone 14 Pro。以下记录后续修复；未跟踪的 Xcode `xcuserdata` 属于本机用户状态，不纳入提交。

# 15. 附件/语音不在模型栏，上传截图仅显示文件名

- **出现情况 / Case**：用户 11:39 截图中，模型与访问模式一行，下面单独一行只有裸露的回形针和麦克风；上传 JPG 显示 UUID 文件名，没有缩略图。
- **原因**：上一轮把按钮移动到了额外的 HStack，去掉了按钮背景；附件栏所有类型都用 doc 图标。临时发送消息也把所有附件标记成 file。
- **修复位置**：`ContentView.swift` 的 `attachmentAndVoiceControls` 合入模型栏横向 ScrollView，按钮恢复胶囊背景；`AttachmentThumbnail` 在附件栏与用户消息中共用，按主机和路径缓存；`AppViewModel.swift` 的 `AttachmentImageCache` 使用 ImageIO 在后台生成有尺寸上限的缩略图，`attachPhoneImage` 先缓存后展示；`Models.swift` 的 `RemoteFileEntry.isImage` 识别图片，`submitPrompt` 保留图片类型。上传与历史图片预览仍通过现有鉴权 API，不增加匿名图片地址。
- **相关修复**：照片 JPEG 转换移到后台，选择器复位成 nil 时直接返回，避免上传成功后又显示“无法读取所选照片”。上传期间切换主机时通过连接代数阻止附件进入错误主机。

# 16. 长草稿越过输入框，键盘展开时工具栏和聊天文字重叠

- **出现情况 / Case**：粘贴多段长文本后，草稿越过输入边框；键盘升起时消息仍位于输入区后面，附件和工具栏压住错误气泡/回复。
- **原因**：聊天和输入区放在同一个 ZStack，聊天只用固定 152/196 pt 占位。实际高度取决于附件、草稿、键盘与字号，固定占位无法正确布局。TextEditor 的 fixedSize 与高度限制也冲突。
- **修复位置**：`ContentView.swift` 的根布局改为 VStack，聊天与 composer 分占空间，移除固定大占位及手动键盘高度/负 padding；聊天区域裁剪、使用系统 sizeChanges 底部锚点。`ComposerTextInput` 使用 UITextView 的 `sizeThatFits`，按当前字体最多五行，高出部分在控件内部滚动并裁剪；焦点由原生代理与普通 State 同步。
- **回归发现**：自动截图发现 SwiftUI 多行 TextField 加 fixedSize 仍可能绘制到框外，因此最终改用明确尺寸的 UITextView；另一次测试抓到 FocusState 与原生 firstResponder 不匹配导致首字后键盘收起，最终用 State/UITextViewDelegate 修复。测试同时核对实际草稿全文，不能只验证输入框存在。

# 17. 输入、键盘展开收起与图片处理卡顿

- **出现情况 / Case**：长对话中编辑草稿、上拉键盘和缩回输入区不流畅，上传截图时也可能阻塞界面。
- **原因**：原先每次修改 `AppViewModel.@Published draft` 都使订阅该对象的聊天/消息树更新；根视图还监听键盘通知改变额外 padding，滚动结束查询强制 `layoutIfNeeded`。图片转换与解码也会占用主线程。
- **修复位置**：`AppViewModel.swift` 的 `ComposerDraft` 独立发布草稿变化；`ContentView.swift` 的 `ComposerDraftScope` 只更新输入区域。移除聊天和首页的手动键盘高度监听，移除 `ChatScrollController.isAtBottom` 中不必要的同步布局；图片编码和缩略图解码移到后台。保留用户主动跳转、历史分页等确有需要的滚动定位。
- **验证范围**：UI 回归覆盖持续长输入、完整拖动收起键盘、重新聚焦以及输入/工具栏/聊天区域几何边界；没有仪器级真机帧率数据，不能承诺固定 FPS。

# 18. 首页项目灰色无选中反馈，项目之间缺少明确边框

- **出现情况 / Case**：CV、Calcu 折叠时只是灰色文字，点开后仍像普通标题；用户追加要求透明蓝色框。
- **修复位置**：`SidebarView.swift` 把项目标题放进各自独立的 insetGrouped section，默认保持收起；共用 `projectHeader` 在展开时显示蓝色实心文件夹，标题保持主文本色，提供展开/收起 accessibilityValue；`projectCardBackground` 使用低不透明度的无色材质与首尾圆角，项目间设置 14 pt 间距。手机和 iPad 共用样式。输入框使用低不透明度材质，聚焦时描边加亮。
- **17:48 截图追加 Case**：对每行分别 stroke 会在长项目中形成蓝色表格，淡蓝色填充也不符合用户要的透明效果。移除逐行蓝色描边和所有项目蓝色填充，恢复原生淡分隔线；新对话按钮改为较小的中性文字，页面使用系统中性背景。蓝色只点缀展开的文件夹，选中任务使用轻微中性色强调。保留原生 List，因此归档滑动操作不受自定义卡片容器影响。

# 19. 桥已经运行，但 Desktop 仍使用旧后端，writer 冲突持续

- **出现情况 / Case**：部署桥之后用户继续在手机发消息，仍得到“此会话正由其他客户端占用写入权”，电脑仅切换过任务。
- **实际证据**：本轮检查 Desktop PID 20811 自 2026-09-24 22:22:42 运行，内置 app-server PID 20849 也未换；8891 上只有桥的 LISTEN，没有 Desktop ESTABLISHED 连接。此前设置的 launchctl 变量不会改变已运行进程，上一轮部署不能等同于接入完成。
- **修复位置**：`apps/server/bin/connect-desktop.js --check` 先初始化真实共享服务，再检查 Desktop 主进程是否实际建立共享端口连接；`--restart` 正常退出 Desktop，等待退出后显式传入连接环境启动并核对连接；不会强杀。`scripts/connect-codex-desktop.command` 提供从 Finder/Terminal 双击的入口，避免当前任务退出时连带终止重启脚本。`--rollback` 可取消当前登录会话环境覆盖并重开原后端；要永久停用，还需卸载现有桥 LaunchAgent。
- **桥细节**：`desktop-bridge.js` 在鉴权后暂停客户端读取，等 Unix socket 建立再 pipe，避免连接空窗丢帧；上游连接有超时并在关闭时清理客户端。
- **版本与协议验证**：Desktop 内置 `0.155.0-alpha.16.4`、受管 daemon `0.157.1`；`probes/shared-app-server.mjs --unix-bridge` 现在直接启动正式桥代码，在独立 CODEX_HOME 中使用本地模拟模型，验证同任务双客户端交替发送、完成事件双方接收和退订后继续发送。不会写真实会话或产生真实项目。
- **当前边界**：完整切换必须结束正在进行的 Desktop 任务并重启应用；不能用切换页面替代，也不能通过删锁强抢真实 writer。连接建立之后还需核对 Desktop 工具与审批等 GUI 行为，协议测试并不覆盖所有桌面功能。
- **17:51 状态更新**：再次执行 `node apps/server/bin/connect-desktop.js --check` 返回 0，真实共享服务 initialize 成功，且检测到 Desktop 主进程的共享端口 ESTABLISHED 连接。旧的“Desktop 尚未切换”状态已过时。本任务没有退出 Desktop，也没有向用户真实任务发送验证消息；工具/审批与双向真实发送仍需验收。

**本次 UI 验证入口**：`apps/ios-native/CloudexUITests.swift` 与共享 scheme `CloudexUITests`。使用 `xcodebuild -project apps/ios-native/CloudexNative.xcodeproj -scheme CloudexUITests -destination 'platform=iOS Simulator,id=<UDID>' -collect-test-diagnostics never test`。`--ui-fixture` 只编译进 Debug 模拟器，以内存数据建立 CV/Calcu 和图片，不访问真实服务器，也不写 Codex 会话。没有 Simulator.app 的 GUI 不妨碍 xcodebuild 驱动 XCTest；此前以此为由停止视觉验证是不充分的。

**第 15–19 项验证与交付**：服务端 30/30 测试通过，正式桥的隔离双客户端验证通过。UI 测试覆盖项目折叠/展开、附件/语音/模型同排、图片缩略图、12 行草稿全文保留、五行高度上限、聊天与输入区不重叠、拖动收起键盘及重新聚焦；iPhone 18 Pro / iOS 27 模拟器通过并检查截图。Release 真机签名构建完成。安装到 iPhone 14 Pro 时设备为 unavailable，devicectl 返回 CoreDeviceError 4016；本次新版尚未安装，不能把模拟器结果说成真机验收。没有测量真机 FPS。服务端在 UI 改色后未重复全量测试；没有启动审查子代理或做哈希检查。

# 20. 首页和会话上下滑动卡顿

- **出现情况 / Case**：首页展开含多条 session 的项目后上下滑动卡顿；进入每个 session 后浏览历史也卡。尤其长 Markdown 回复包含代码、列表和表格时，行进入可见区要重复做解析与布局。
- **代码开销**：`SidebarView.projectCardBackground` 为每条原生 List 行生成独立的 ultraThinMaterial 模糊层；`AdaptiveConversationLayout.sizeThatFits` 即使助手气泡已确定为整行宽度，仍先以无限宽测量全文再按实际宽度测量；`MarkdownText.body` 每次求值都重新拆块和解析 AttributedString，表格也单独解析。`updateChatContent` 恢复底部跟随时即使快照没变也会安排一次自动滚动。
- **修复位置**：`SidebarView.swift` 将每行实时模糊改为系统颜色的透明度填充，保留透明卡片、原生列表复用和滑动归档；`ContentView.swift` 的整行气泡跳过无约束测量；`MarkdownText` 用 NSCache 缓存按完整源文本区分的块结构及行内解析，设条数和估算成本限制，系统可回收。普通文本/Markdown 分开键，搜索高亮和颜色在缓存取出后应用，流式增量的新文本使用新键，表格复用相同入口。`updateChatContent` 对相同快照直接返回，不干扰手指的自然滚动。
- **基线及范围**：Codex daemon `0.157.1` / Desktop 内置 `0.155.0-alpha.16.4`；本项只改 iOS 渲染，不依赖新的 Codex 协议。Debug 模拟器测试使用 80 条项目会话、36 个混合 Markdown 回合的内存数据，不创建真实 Codex 项目。`CloudexUITests.testHomeScrollPerformance` / `testConversationScrollPerformance` 对相同上下滑动测量滚动减速和 App CPU，比较优化前后；模拟器测量不代表 iPhone 真机帧率。
- **实际结果**：相同四次滑动、三次测量的平均 App CPU 用时：会话 `2.5800 → 2.4541 s`（约下降 4.9%），首页 `1.2829 → 1.3516 s`（约增加 5.4%，没有测到改善）；两者减速区间均约 2.43 s，该区间时长由手势决定，不是帧率指标。因此不能写“卡顿已完全解决”或将去掉模糊直接换算成 FPS 收益。原始报告为 `/tmp/cloudex-scroll-before.xcresult` 与 `/tmp/cloudex-scroll-after.xcresult`，测量期间未并行构建。
- **补充排查**：持续滑动时对 App 做 15 秒主线程采样，结果在 `/tmp/cloudex-conversation-scroll-sample.txt`。样本包含 SwiftUI/UIKit 布局与 XCTest 无障碍遍历开销，主线程也有大量等待事件的样本；没有证据据此把真实手机的所有卡顿归因于单一函数。采样轮不与前面的无采样 CPU 数值混算。
- **回归与交付**：两个滚动性能场景、原有长输入/键盘/缩略图场景通过；另加 `testScrollToLatestAfterBrowsing`，确认向历史滚动后显式“滚动到最新消息”仍有效，截图核对列表、引用、代码、链接和表格保留。相同快照去重对显式导航放行。Release 签名构建通过，18:05 已通过 devicectl 成功安装到 iPhone 14 Pro；真机连续滑动体感及帧率仍待验证。本轮未改服务端，因此没有重复其 30 项测试；无新依赖、审查子代理或哈希流程。

# 21. 偶尔滚到底部出现整片空白，需要回拉才能看到末尾

- **出现情况 / Case**：手机向最新消息方向滚动到底部后，聊天可见区偶尔变成一片白；反向拉回后才看到真正的最后一条回复。不能把这种持续留白当成正常的短暂弹性回弹。
- **发现的定位冲突**：`ContentView.swift` 原来的底部请求先用 `ScrollViewReader.scrollTo("chat-bottom")` 定位，再以 `UIScrollView.contentSize` 算偏移；显式回到底部还在 0、80、250、500 ms 多次重启动画，首次打开也有原生偏移纠正。LazyVStack 未测量的行包含高度估算，长短消息混排时估算值会变化，两套定位可能互相覆盖，把滚动位置推过实际底部。这是代码确认的风险路径；旧版在本轮模拟器压力测试中未稳定复现用户的偶发现象，不能宣称已证明唯一根因。
- **修复位置**：`ContentView.chat` 的 `scrollToBottomRequest` 统一使用末尾锚点；首次打开和主动“回到最新”以无动画事务定位，正常新内容跟随保留短动画。删除重复定时补滚、`ChatScrollController.scrollToBottom` 以及首次布局完成时按估算高度重设偏移的逻辑。保留键盘 viewport 调整、明确的历史定位和用户手势；不会把每次向上读历史都强拉回底部。
- **验证 Case**：`CloudexUITests.testBottomRemainsVisibleAfterRepeatedFlicks` 使用 36 条长短交替消息（长消息 28 段、短消息 2 段、末尾仅一句），重复三轮“浏览历史 → 回到最新 → 连续三次快速滑到底部”，检查最后一条仍可点击且距聊天 viewport 底端小于 100 pt，并保存各轮截图。另跑既有的最新消息跳转、长输入和键盘回归；这些测试验证修复后边界，不能代替用户真机偶发问题的复现证据。
- **Codex 版本机制**：仍基于 daemon `0.157.1` / Desktop 内置 `0.155.0-alpha.16.4`。本项是 iOS LazyVStack 与 UIScrollView 定位问题，不涉及 Codex writer、JSONL 索引或服务器协议变更。
- **交付结果**：上述三项 UI 回归全部通过，三轮快速滑动后的截图均保留实际末尾且无整屏留白；Release 签名构建通过，18:13 成功安装到 iPhone 14 Pro。测试报告 `/tmp/cloudex-bottom-after.xcresult`。真机偶发问题是否完全消失仍需后续使用确认；未重复无关的服务端测试或滚动 CPU 基准。
